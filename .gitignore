node_modules/
.DS_Store
*.log
dist/

# Unsent outbound correspondence — kept local, never committed (see ticket 26)
.scratch/ev-guide-spec/correspondence/

# Operator research withheld from the public repo (ticket 26) — kept local only
.scratch/ev-guide-spec/research/03-operator-landscape-and-data-access.md

# build effort
node_modules/
dist/
.turbo/
*.tsbuildinfo

# Flutter (ADR-0012): the native runners are REAL, authored, committed projects.
.dart_tool/
packages/dart/*/pubspec.lock
