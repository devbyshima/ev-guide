{
  "name": "@ev-guide/ui",
  "version": "0.0.0",
  "description": "Design tokens and the pure style layer. Tokens only: the React Native components were deleted with the Expo app (ADR-0012), and the admin SPA that consumes this package takes tokens only (ADR-0006).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
